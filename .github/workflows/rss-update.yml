name: RSS Ingestion

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "rss-ingestion"
  cancel-in-progress: true

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Controlled Groq Test
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_MODEL: openai/gpt-oss-20b
          GROQ_FALLBACK_MODEL: openai/gpt-oss-120b
        run: |
          python scripts/test_groq_pipeline.py

      - name: Run Intel Balancer (RSS Fetcher)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_MODEL: openai/gpt-oss-20b
          GROQ_FALLBACK_MODEL: openai/gpt-oss-120b
        run: |
          python scripts/rss_fetcher.py

      - name: Run CVE Fetcher (NVD + KEV)
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GROQ_MODEL: openai/gpt-oss-20b
          GROQ_FALLBACK_MODEL: openai/gpt-oss-120b
        run: |
          python scripts/cve_fetcher.py

      - name: Commit Content Changes
        run: |
          git config user.name "DedSec-Terminal Bot"
          git config user.email "actions@github.com"
          git add content/posts data/rss_state.json content/cves data/cve_state.json

          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore(content): Automated Intelligence & CVE Update"
            git push
          fi

  deploy:
    needs: fetch
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
